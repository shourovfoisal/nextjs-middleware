/**
 * function name should be middleware
 * any other name would also work
 * but in that case, its export has to be default
 */
export function middleware() {
    console.log("hello from the middleware.. again..");
}